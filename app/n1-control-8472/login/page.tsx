'use client';
import {useState} from 'react';
import {LockKeyhole,ArrowRight,ShieldCheck} from 'lucide-react';
export default function Login(){const [err,setErr]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr('');const f=new FormData(e.currentTarget);try{const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:f.get('username'),password:f.get('password')})});const j=await r.json().catch(()=>({}));if(!r.ok){setErr(j.error||'Login yoki parol noto‘g‘ri');return}location.href='/n1-control-8472'}catch{setErr('Server bilan ulanishda xato.')}finally{setBusy(false)}}return <main className="loginPage"><form className="loginBox" onSubmit={submit}><a className="brand" href="/"><span>N1</span> Lavash N1</a><div className="featureIcon" style={{marginTop:28}}><LockKeyhole/></div><h1>Boshqaruv paneli</h1><p>Lavash N1 buyurtmalari, menyu va sayt sozlamalarini boshqaring.</p>{err&&<div className="alert">{err}</div>}<input name="username" placeholder="Admin login" autoComplete="username" required/><input type="password" name="password" placeholder="Parol" autoComplete="current-password" required/ ><button className="primary full" disabled={busy}>{busy?'Tekshirilmoqda...':<>Kirish <ArrowRight size={18}/></>}</button><small style={{color:'#777',display:'flex',gap:7,alignItems:'center'}}><ShieldCheck size={15}/> Himoyalangan admin kirishi</small></form></main>}
