'use client';
import {useState} from 'react';
import {LockKeyhole,ArrowRight,ShieldCheck} from 'lucide-react';
import {useRouter} from 'next/navigation';
export default function Login(){const [u,setU]=useState(''),[p,setP]=useState(''),[err,setErr]=useState('');const r=useRouter();function go(e:React.FormEvent){e.preventDefault();if(u==='Daler'&&p==='Daler'){sessionStorage.setItem('n1-admin','yes');r.push('/n1-control-8472')}else setErr('Login yoki parol noto‘g‘ri.')}return <main className="adminLogin"><form className="loginBox" onSubmit={go}><div className="logo">Lavash<span>N1</span></div><div className="featureIcon"><LockKeyhole/></div><h1>Admin panel</h1><p className="muted">Lavash N1 boshqaruv markazi</p>{err&&<div className="error">{err}</div>}<label className="field"><b>Login</b><input value={u} onChange={e=>setU(e.target.value)} placeholder="Admin login" autoComplete="username"/></label><label className="field"><b>Parol</b><input type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="••••••••" autoComplete="current-password"/></label><button className="save" style={{width:'100%',display:'flex',justifyContent:'center',gap:8}}>Kirish <ArrowRight size={18}/></button><p className="muted" style={{fontSize:12,display:'flex',gap:6,alignItems:'center'}}><ShieldCheck size={15}/> Maxsus boshqaruv sahifasi</p></form></main>}