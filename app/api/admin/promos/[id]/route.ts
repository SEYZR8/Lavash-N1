import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const b=await req.json();return NextResponse.json(await prisma.promo.update({where:{id:Number(id)},data:{code:String(b.code||'').trim().toUpperCase(),title:String(b.title||'').trim(),percent:Math.max(0,Math.min(100,Number(b.percent||0))),fixed:Math.max(0,Number(b.fixed||0)),minOrder:Math.max(0,Number(b.minOrder||0)),active:b.active!==false}}))}catch{return NextResponse.json({error:'Promo topilmadi'},{status:404})}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;await prisma.promo.delete({where:{id:Number(id)}});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Promo topilmadi'},{status:404})}}
