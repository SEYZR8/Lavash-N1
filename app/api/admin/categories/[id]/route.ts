import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const b=await req.json();return NextResponse.json(await prisma.category.update({where:{id:Number(id)},data:{name:String(b.name||'').trim(),icon:String(b.icon||'🍽️'),sortOrder:Number(b.sortOrder||0),active:b.active!==false}}))}catch{return NextResponse.json({error:'Kategoriya topilmadi'},{status:404})}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;await prisma.category.update({where:{id:Number(id)},data:{active:false}});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Kategoriya topilmadi'},{status:404})}}
