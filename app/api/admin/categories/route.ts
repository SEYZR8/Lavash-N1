import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {slugify} from '@/lib/utils';
export async function GET(){return NextResponse.json(await prisma.category.findMany({orderBy:{sortOrder:'asc'}}),{headers:{'Cache-Control':'no-store'}})}
export async function POST(req:Request){try{const b=await req.json();const name=String(b.name||'').trim();if(!name)return NextResponse.json({error:'Kategoriya nomi kerak'},{status:400});return NextResponse.json(await prisma.category.create({data:{name,slug:b.slug||slugify(name)+'-'+Date.now().toString(36),icon:String(b.icon||'🍽️'),sortOrder:Number(b.sortOrder||0),active:b.active!==false}}),{status:201})}catch(e:any){return NextResponse.json({error:e?.code==='P2002'?'Slug mavjud':'Saqlashda xato'},{status:400})}}
