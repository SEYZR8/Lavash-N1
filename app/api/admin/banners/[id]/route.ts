import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const b=await req.json();return NextResponse.json(await prisma.banner.update({where:{id:Number(id)},data:{title:String(b.title||'').trim(),subtitle:String(b.subtitle||''),button:String(b.button||'Buyurtma berish'),image:String(b.image||''),active:b.active!==false,sortOrder:Number(b.sortOrder||0)}}))}catch{return NextResponse.json({error:'Banner topilmadi'},{status:404})}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;await prisma.banner.delete({where:{id:Number(id)}});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Banner topilmadi'},{status:404})}}
