import {NextResponse} from 'next/server';import {ADMIN_COOKIE,adminToken,validCredentials} from '@/lib/auth';
export async function POST(req:Request){try{const {username,password}=await req.json();if(!validCredentials(String(username||''),String(password||'')))return NextResponse.json({error:'Login yoki parol noto‘g‘ri'},{status:401});const res=NextResponse.json({ok:true});res.cookies.set(ADMIN_COOKIE,adminToken(),{httpOnly:true,sameSite:'strict',secure:process.env.NODE_ENV==='production',path:'/',maxAge:60*60*12});return res}catch{return NextResponse.json({error:'Noto‘g‘ri so‘rov'},{status:400})}}
